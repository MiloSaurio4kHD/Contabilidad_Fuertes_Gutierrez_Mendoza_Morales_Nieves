namespace Contabilidad
{
    /// <summary>
    /// Indica si un formulario de Agregar/Editar esta operando en modo de creacion o edicion,
    /// para que el propio formulario ajuste su titulo y comportamiento segun corresponda.
    /// </summary>
    public enum ModoFormulario
    {
        Crear,
        Editar
    }
}
